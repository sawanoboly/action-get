name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: sawanoboly/action-set@master
      with:
        who-to-greet: Hello 
    - name: Run an env
      run: echo ${{ outputs.time }}
    - name: Run a multi-line script
      run: |
        echo Add other actions to build,
        echo test, and deploy your project.
